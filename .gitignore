# === Secrets ===
.env
*.token
*.key
*.pem

# === Private email data (NEVER commit) ===
maildata/**
!maildata/README.md
*.mbx
*.mbox
*.eml
*.pst

# === Index / derived data (may contain email-derived content) ===
index/**
!index/.gitkeep

# === Proton Bridge vault / keychain (credentials — NEVER commit) ===
bridge-data/**
!bridge-data/.gitkeep

# === Models / weights ===
*.gguf
*.safetensors
*.bin
models/

# === Python / build ===
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
node_modules/

# === Logs / scratch ===
*.log
.DS_Store

# === Local compose override (LAN test bind) ===
docker-compose.override.yml
