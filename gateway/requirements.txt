# MCP gateway + ingest
mcp>=1.2.0
starlette>=0.37
uvicorn[standard]>=0.30
openai>=1.40            # OpenAI-compatible clients for Qwen / xinference / safeguard
lancedb>=0.15
pyarrow>=16
pydantic>=2.7
httpx>=0.27
beautifulsoup4>=4.12    # HTML body -> text
lxml>=5
python-dateutil>=2.9
tqdm>=4.66
pypdf>=4.2              # attachment text extraction (PDF)

# In-container safeguard server (gpt-oss-safeguard GGUF). Built with CUDA in the image.
# Pinned loosely; the Dockerfile installs the CUDA wheel.
llama-cpp-python[server]>=0.3.2
